# Security policy

This module suppresses a security-warning UI and loads into a privileged system service. Report compatibility failures, unexpected process crashes, over-broad hooks, or scope escapes through the repository host's private security-advisory feature.

Include the device build, target APK SHA-256, LSPosed version and relevant module logs. Do not upload the proprietary target APK or private device dumps to a public issue.
